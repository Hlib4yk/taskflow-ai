# TEMPLATE ONLY — placeholder values so the manifest applies cleanly for a
# demo cluster. Replace with real secrets before any non-local deployment,
# ideally via sealed-secrets / external-secrets / your cloud's secret
# manager rather than committing plaintext values to git.
apiVersion: v1
kind: Secret
metadata:
  name: taskflow-secrets
  namespace: taskflow-ai
type: Opaque
stringData:
  POSTGRES_USER: "taskflow"
  POSTGRES_PASSWORD: "change-me"
  GATEWAY_DATABASE_URL: "postgresql://taskflow:change-me@postgres:5432/authdb"
  TASKS_DATABASE_URL: "postgresql://taskflow:change-me@postgres:5432/tasksdb"
  RABBITMQ_USER: "taskflow"
  RABBITMQ_PASSWORD: "change-me"
  RABBITMQ_URL: "amqp://taskflow:change-me@rabbitmq:5672"
  JWT_ACCESS_SECRET: "change-me-access-secret"
  JWT_REFRESH_SECRET: "change-me-refresh-secret"
  ANTHROPIC_API_KEY: "sk-ant-xxxxx"
  BULL_BOARD_USER: "admin"
  BULL_BOARD_PASSWORD: "change-me"
